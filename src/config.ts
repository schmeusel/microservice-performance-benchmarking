export default {
    logging: {
        measurements: {
            filepath: __dirname + '/logs/measurements.log'
        },
        systemEvents: {
            filepath: __dirname + 'logs/systemEvents.log'
        }
    }
};
