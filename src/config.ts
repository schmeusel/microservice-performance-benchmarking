import * as path from 'path';

export default {
    logging: {
        measurements: {
            filename: 'measurements.log'
        },
        systemEvents: {
            filename: 'systemEvents.log'
        }
    }
};
